# Shell scripts and systemd/sudoers files must keep LF endings or Linux
# refuses to run them ("bad interpreter: /usr/bin/env bash\r").
*.sh        text eol=lf
*.service   text eol=lf
*-sudoers   text eol=lf
